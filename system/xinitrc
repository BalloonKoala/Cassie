#!/bin/sh
# Cassie 2.0 — one Python process (pygame orb + AI). No Chromium.
export DISPLAY=:0

xset s off
xset -dpms
xset s noblank
xsetroot -solid '#000000'

exec /opt/cassie/venv/bin/python3 /opt/cassie/src/run.py
